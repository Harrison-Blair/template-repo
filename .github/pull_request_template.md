## Summary of changes

-

## Documentation updates

- No docs impact

## Testing / verification

-

## Linked issues

Closes #

## Pre-merge checklist

- [ ] `npm run lint` passes (or N/A)
- [ ] `npm run build` passes (or N/A)
- [ ] Python changes pass `ruff check .` and `mypy .` (or N/A)
- [ ] Docs updated per the table in `CLAUDE.md` (or N/A)
- [ ] No secrets committed (no `.env*`, `*.key`, `*.pem`, credentials, data under `private/`, `analytics/`, `secrets/`, `data/raw/`)
- [ ] Self-reviewed the diff
